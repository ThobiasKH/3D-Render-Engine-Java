package BaseComponents;

public class DirectionalLight extends AbstractLight {
    // int color;
    // int intensity;
    // Vector3 direction;

    public DirectionalLight(float intensity, Vector3 direction) {
        super(intensity, direction);
    }
}
